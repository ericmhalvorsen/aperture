# Vanilla + Aperture Example

No-framework Vite app using the standalone `<script>` tag integration method.

```bash
pnpm install
pnpm --filter vanilla-aperture run dev
```

Visit `http://localhost:5173` and connect your MCP client.
