import type { NextConfig } from "next";
import { withAperture } from "@ericmhalvorsen/aperture/next";

const nextConfig: NextConfig = {
	/* config options here */
};

export default withAperture(nextConfig);
