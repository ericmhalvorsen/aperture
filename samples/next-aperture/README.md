# Next.js + Aperture Example

Barebones Next.js app with Aperture pre-configured. Includes a custom tool (`get_dummy_data`) to demonstrate the custom tool API.

```bash
pnpm install
pnpm run example
```

Visit `http://localhost:3000` and connect your MCP client.
