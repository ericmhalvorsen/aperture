# Vite + React + Aperture Example

Vite + React app with Aperture integrated via the Vite plugin.

```bash
pnpm install
pnpm --filter vite-aperture run dev
```

Visit `http://localhost:5174` and connect your MCP client.
