[tools]
node = "20"
pnpm = "latest"

[tasks.build]
description = "Build the Aperture project"
run = "pnpm run build"

[tasks.dev]
description = "Build and watch the Aperture project"
run = "pnpm run dev"
